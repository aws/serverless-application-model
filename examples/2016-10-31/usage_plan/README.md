# Usage Plans Support for Auth

Example SAM template for creating Usage Plans.
## Running the example

```bash
$ sam deploy \
    --template-file /path_to_template/packaged-template.yaml \
    --stack-name my-stack \
    --capabilities CAPABILITY_IAM
```
