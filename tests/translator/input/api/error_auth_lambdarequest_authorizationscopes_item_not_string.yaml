# AuthorizationScopes is not a list
Transform: AWS::Serverless-2016-10-31
Resources:
  MyApi:
    Type: AWS::Serverless::Api
    Properties:
      StageName: Stage name
      Auth:
        Authorizers:
          MyLambdaRequestAuth:
            AuthorizationScopes:
              - 3
            FunctionArn: Function.arn
