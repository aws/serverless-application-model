Resources:
  MyApi:
    Type: AWS::Serverless::HttpApi
    Properties:
      Auth:
        EnableIamAuthorizer: true
        DefaultAuthorizer: AWS_IAM
      DefinitionBody:
        info:
          version: '1.0'
          title:
            Ref: AWS::StackName
        openapi: 3.0.1
        paths: {}
Metadata:
  SamTransformTest: true
